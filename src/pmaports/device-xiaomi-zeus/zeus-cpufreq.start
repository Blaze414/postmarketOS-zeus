#!/bin/sh
# Apply the saved CPU clock profile at boot. Defaults to "conservative" (big
# clusters capped for heat and battery) when nothing has been chosen. Switch
# with: zeus-cpu-profile performance | conservative
exec /usr/bin/zeus-cpu-profile boot
